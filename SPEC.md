# 文化祭シューティングゲーム 仕様書

プロジェクター投影 × スマホコントローラーで遊ぶシューティングゲーム。

## 全体方針

- サーバー: Node.js + Express + ws（WebSocket）
- クライアント（プロジェクター側画面）: Three.js（CDN / importmap 読み込み、ビルド不要）
- 入力: 最終的にはスマホをコントローラーにするが、まずはマウス入力で実装し、
  後から差し替えられるように `InputSource` という抽象を設ける

## Phase 0: プロジェクト基盤

- Node.js のバージョンを確認する
- プロジェクト直下に `package.json` を作成する
  - 依存パッケージは `express` と `ws` のみ
  - `npm start` で `server.js` が起動するようにする
- `server.js` はこの時点では中身を空（起動するだけ）でよい

## Phase 1: 一人称シューティング（マウス操作版）

`public/index.html` と `public/game.js` を作成する。

- Three.js は CDN（importmap）で読み込み、ビルド不要にする

### ゲーム内容

- 一人称固定視点（プレイヤーは移動しない）
- 正面に的（ターゲット）を5個並べて配置する
- マウスカーソルの位置にクロスヘアを描画する
- クリックで弾（球体）を発射する
  - 弾には重力を適用し、放物線を描いて飛ぶ
- 弾が的に当たったら、その的を消して 100点 加算する
- 画面上部に以下を常時表示する
  - 現在のスコア
  - 残り時間（制限時間90秒からのカウントダウン）
- 制限時間が0になったらリザルト画面を表示する
  - リザルト画面をクリックするとゲームを最初から再スタートする

### 入力抽象化（InputSource）

スマホ入力への差し替えを見据え、入力処理を `InputSource` という抽象インターフェースとして切り出す。

- `getAim()`: 現在の照準位置を `{x, y}`（-1〜1に正規化した座標）で返す
- `consumeFire()`: 発射リクエストがあったかどうかを返す（呼び出すと消費される）

この段階では `InputSource` の実装として `MouseInput` のみを実装する
（マウス座標を正規化して `getAim()` に反映し、クリックで `consumeFire()` が
true を返すようにする）。

## Phase 2以降（将来対応・今回は未着手）

- スマホをコントローラーとして使う `InputSource` 実装（WebSocket 経由で
  照準・発射をサーバー/表示画面に送る想定）
